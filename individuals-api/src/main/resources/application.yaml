server:
  port: ${APP_PORT}
spring:
  application:
    name: ${APP_NAME}
  profiles:
    active:
      - dev
  main:
    web-application-type: reactive
management:
  otlp:
    metrics:
      export:
        enabled: false
  metrics:
    tags:
      application: ${APP_NAME}
  endpoints:
    web:
      exposure:
        include:
          - prometheus
          - info
          - health