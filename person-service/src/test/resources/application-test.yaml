spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations:
      - classpath:db/test-migration
    create-schemas: true
    schemas:
      - person
      - public
      - service
  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: validate